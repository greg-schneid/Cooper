#ifndef ENUMS_H

#define ENUMS_H

enum leg{
    left_rear = 0,
    right_rear = 1,
    left_front = 2,
    right_front = 3
};

enum servoType{
    abductor = 0,
    hip = 1,
    knee = 2
};

#endif //ENUMS_H